<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pj2.shoecream.mapper.SchedulerMapper">
    <select id="selectTrackingList" resultType="map">
        select
            *
        from
            courier_info
        where
            delivery_status NOT IN ('배송완료','인수')
    </select>
    <update id="updateTracking">
        update
            courier_info
        set
            delivery_status = #{kind }
            , update_time = now()
        where
            tracking_num = #{trackingNumber }
    </update>
</mapper>
